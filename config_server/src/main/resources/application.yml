server:
    port: 8081
    
spring:
    application:
        name: config_server
        
cloud:
    config:
        server:
            git:
                uri: https://github.com/NoeliaPodmoguilny/config-server-prueba.git
                searchPaths: config-data
                username: ${GIT_USER}
                password: ${GIT_PASSWORD}